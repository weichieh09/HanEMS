entity Equipment {
	eqItemId Long,
	name String,
	description String,
	createDate Instant,
	modifyDate Instant,
}

filter *
dto * with mapstruct
paginate * with pagination
service * with serviceClass
